# AGENTS.md

## Cursor Cloud specific instructions

This repository (`agent-example`) is currently an empty scaffold with only a `README.md` and `LICENSE` (MIT) file. There is no source code, no dependency manifest, no build system, no services, and no tests.

- **No dependencies to install** — no `package.json`, `requirements.txt`, `Cargo.toml`, `go.mod`, or similar exists.
- **No lint/test/build/run commands** — no tooling is configured yet.
- **No services** — nothing to start or connect to.

When source code is added in the future, this section should be updated with relevant dev environment instructions.
